---
"eslint-config-yarapa": major
---

Require the canonical `eslint.config.mjs` template and publish `yarapa-eslint-config` for CI enforcement. Consumer repositories must remove local ESLint rules, ignores, globals, and other config additions.
